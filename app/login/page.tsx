import { RoutePlaceholder } from "@/components/placeholders/route-placeholder";

export default function LoginPage() {
  return (
    <RoutePlaceholder
      title="Login"
      description="Sign in to manage customers, visits, rewards, and campaigns."
    />
  );
}
